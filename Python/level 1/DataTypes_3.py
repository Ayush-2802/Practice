# Write a program that converts a float to an integer, then checks the types of both variables (float and integer)

nfloat = 1.0
nint = int(nfloat)

print(f"the nfloat type {type(nfloat)}")
print(type(nint))